
  # UiX Encuestas

  This is a code bundle for UiX Encuestas. The original project is available at https://www.figma.com/design/PfXw85H8dbeypmv2TKvIKC/UiX-Encuestas.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  